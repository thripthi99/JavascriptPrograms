// 3. program to append an object to an arr

function insertObject(arr, obj) {

  arr = [...arr, object];

  console.log(arr);
}
let array = [1, 2, 3];
let object = { x: 12, y: 8 };
insertObject(array, object);